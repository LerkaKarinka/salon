<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <title>Сотрудники</title>
</head>

<body>
    <h1>Сотрудники</h1>
    
  
  <ul>
  {% for employee in emp %}
    <li>
      <em>{{ employee.id }}:</em> {{ employee.name }}: {{employee.post}}
    </li>
  {% endfor %}
  </ul>
</body>
</html>